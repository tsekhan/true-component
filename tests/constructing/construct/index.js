const name = 'Construction';

const description = 'Tests if object creates successfully';

const run = async () => {
  return {
    status: 'success',
    message: 'Success run',
  };
};

export default {
  name,
  description,
  run,
};
